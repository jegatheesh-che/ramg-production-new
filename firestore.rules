rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper function to verify if the request is from an authenticated Admin
    function isAdmin() {
      return request.auth != null;
    }

    // Rules for Gallery Collection
    match /gallery/{document=**} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Rules for Reviews Collection
    match /reviews/{document=**} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Rules for About Collection (Section 1 & 2 Content)
    match /about/{document=**} {
      allow read: if true;
      allow create, update, delete: if isAdmin();
    }

    // Deny all other collections by default for security
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
